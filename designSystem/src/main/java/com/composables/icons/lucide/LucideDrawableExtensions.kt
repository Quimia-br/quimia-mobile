package com.composables.icons.lucide

import com.quimia.designsystem.R

// Expose drawable resource via the existing Lucide object as an Int property
val Lucide.ToolCase: Int
    get() = R.drawable.tool_case
